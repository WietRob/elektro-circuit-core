{
  "name": "elektro-circuit-core",
  "version": "2.0.0",
  "description": "Elektrotechnik Schaltplan-Generator Core",
  "main": "src/generator/circuit-generator-v2.js",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check",
    "test": "node tests/runner.js && node tests/state-engine.test.js && node tests/geometry-contract.test.js",
    "test:visual": "npx playwright test tests/visual/ci-minimal.spec.js",
    "test:runtime": "npx playwright test tests/visual/smoke.spec.js tests/visual/folgeschaltung-smoke.spec.js",
    "verify:local": "npm run build && npm test && npm run test:visual",
    "verify:visual:local": "npm run build && npm run test:visual",
    "verify:runtime:local": "npm run build && npm run test:runtime"
  },
  "keywords": [
    "elektrotechnik",
    "schaltplan",
    "generator"
  ],
  "author": "Roberto Schmidt",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1"
  }
}
